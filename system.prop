ro.cwm.forbid_format = /bootloader,/dfs,/backup,/splash,/sdcard
# supposedly unmounting sdcard from recovery can cause a soft brick (I highly doubt this but noone has a reason to unmount it anyway)
ro.cotr.forbid_mount = /splash,/sdcard
